function calculaArea(base, altura) {
    base = prompt("insira o valor da base")
    altura = prompt("insira o valor da altura")
    let resultado = base * altura
    console.log(resultado)
}

calculaArea() //callback?